const letras = ['a', 'b', 'c'];
const letras2 = ['n', 'm', 't'];

const letrastotales = letras.concat(letras2);

console.log(letras);
console.log(letras2);

console.log(letrastotales);

// se utiliza para unir dos o mas arrays. este metodo no cambia los arrays existentes, sino que devuleve un nuevo array
